# 2026-09-09 작업계획

- 날짜: 2026-09-09
- Phase: 0 — Protocol Foundation (`docs/tasks/PHASE_00.md`)
- 목표: 공유 Tool Protocol v1 계약(ID, 에러, JSON Schema, protobuf, 계약 테스트)을 저장소에 고정한다.

## 전제

- `docs/HANDOFF.md`: 문서만 정리됨. 코드 없음. 계획서 작성 후 Phase 0 착수.
- `docs/issues/open.md`: 열린 이슈 없음.
- 최근 로그: 없음.
- 저장소는 docs / AGENTS.md / cursor rules만 존재. `protocol/` 없음.

## 오늘 할 일

- [x] `protocol/` 모듈 생성 (Go 계약 코드 + 언어 독립 스키마)
- [x] 공통 ID 생성/검증 규칙 (`MSG`, `CALL`, `SES`, `PRJ`, `WS`, `WI`, `WF`, `APR`, `CHG` + UUIDv7)
- [x] 구조화 에러 모델과 v1 에러 코드 카탈로그
- [x] `oncode-tool/1.0` 버전 파싱·호환·거부
- [x] 핵심 JSON Schema: Envelope, ToolRequest/Response, ToolError, Heartbeat, Capabilities, Approval, WorkflowEvent, ProposedChangeSet, ActualDiff
- [x] Server ↔ Local Agent용 protobuf (`protocol/schemas/proto`)
- [x] fixtures + 계약 테스트 (JSON round-trip, 버전 거부, proto↔JSON 논리 필드 매핑)
- [x] `docs/issues`에 막히는 점 기록

## 제외

- Local Agent daemon / tool handler
- Spring Boot, Workflow, IDE, LLM, PostgreSQL
- Guide, Nexus, HA
- 개별 tool argument의 완전한 JSON Schema (Phase 1+에서 확장)

## 건드릴 모듈/문서

- `go.mod`, `.gitignore`
- `protocol/**`
- 이슈/로그/HANDOFF는 작업 중·종료 시 갱신

## 완료 기준

`docs/tasks/PHASE_00.md` Definition of Done 1–9.

## 리스크

- protobuf 코드젠(`protoc`)이 없을 수 있음 → `.proto`를 계약으로 두고, 테스트는 필드 매핑과 JSON을 검증 (코드젠은 후속).
- SPEC-01 Envelope에 `workspace_id`가 없음 → PHASE_00 요구대로 Envelope 선택 필드로 추가.
- 외부 JSON Schema 라이브러리 없이 검증 → 스키마 파일 + Go required-field 검증으로 계약 테스트.
