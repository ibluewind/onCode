# 2026-09-16 작업계획

- 날짜: 2026-09-16
- Phase: 2 — Server Workflow Core (`docs/tasks/PHASE_02.md`)
- 목표: 1·2일차에 이어 **남은 Phase 2 DoD(5–7·9)** 를 닫는다.

## 전제

- Phase 0 Protocol Foundation 완료.
- Phase 1 Local Agent Core DoD 충족 (2026-09-14).
- 오픈 이슈 없음.
- 1일차: JDBC+Flyway SSOT, context get/put, WAITING resume/lease.
- 2일차: 설계/코드 승인 분리, 거절·재사용·만료 fail-closed.
- 기록: `docs/results/2026-09-16-구현결과.md`
- git commit/push를 Agent 도구로 추가하지 않음.

## 오늘 범위 (1일차 슬라이스)

문서:

- [x] `02_IMPLEMENTATION_RULES` 적용 범위 명확화: onCode **사용 시** 규칙이지, onCode **자체 개발** 툴체인 규칙이 아님
- [x] `SPEC-14` §63·§150: 내부 Nexus는 사용자 워크스페이스/운영 인스턴스 대상. onCode 저장소 빌드를 강제하지 않음
- [x] 코딩 규칙: DB 네이밍·주석, 클래스/메소드 주석, 언어별 네이밍 (`.cursor/rules/`)

구현:

- [x] `server/` Spring Boot modular monolith 골격
- [x] PostgreSQL 연동 (로컬/테스트용). 워크플로 상태 SSOT
- [x] Work Item / Workflow / Workflow Transition 최소 영속
- [x] 명시적 상태 전이 RECEIVED → WAITING_DESIGN_APPROVAL
- [x] `context.get` / `context.put` 최소 구현
- [x] WAITING_* resume/lease 테스트
- [x] 단위·워크플로 테스트 동반

## 오늘 범위 (2일차 슬라이스 — 승인)

- [x] Flyway V2 `approval`
- [x] `ApprovalPort` 테스트 어댑터
- [x] 설계 승인/거절, 코드 승인/거절
- [x] 재사용·만료·불일치 fail-closed
- [x] WAITING_CODE_APPROVAL 재시작
- [x] 테스트 + 구현결과 슬라이스 2

## 오늘 범위 (3일차 슬라이스 — Tool Gateway · Agent · Inference)

PHASE_02 DoD 5–7·9와 필수 테스트(에이전트 apply 금지, context_ref 교환, Tool Gateway timeout/error, duplicate side-effect)를 닫는다. IDE가 없으므로 ADR-003 gRPC는 붙이지 않고, PHASE_02가 허용한 **인프로세스 Local Agent 하니스**로 Phase 1 도구를 호출한다. 실 vLLM은 붙이지 않고 Inference Gateway만 모델 진입점으로 둔다.

- [x] Inference Gateway: model profile만 받고 완료. 에이전트는 여기를 통하지 않으면 모델을 못 부른다
- [x] Agent Runtime: Definition, prompt, context builder, validator, result writer (`context.put`)
- [x] 경계 에이전트 3개: Senior Developer / Implementation / Review. 서로 payload가 아니라 `context_ref`만 넘긴다
- [x] Tool Gateway: ToolRequest 매핑, `call_id` 상관, timeout·structured ToolError, 파일시스템 직접 접근 없음
- [x] 인프로세스 하니스가 Phase 1 도구명(`system.ping`, `workspace.read_file`, `workspace.propose_changes`)에 응답
- [x] AGENT는 `workspace.apply_changes` 호출 불가 (게이트웨이 거부 + Implementation 허용 목록에 없음)
- [x] 동일 `call_id`의 side-effect 도구는 재실행하지 않음
- [x] mock 루프: User Request → Senior 설계 → 승인 → Implementation propose → Review
- [x] 테스트 + `docs/results/2026-09-16-구현결과.md` 슬라이스 3

하지 않음 (이번에도 스텁/제외):

- Local Agent gRPC 어댑터 (ADR-003 전송. 논리 프로토콜만 Java에서 사용)
- 실 vLLM / 실모델 HTTP
- REST 승인 API, IDE, Guide, Nexus, HA
- V1/V2 DDL 수정, Local Agent/protocol 재설계
- `workspace.apply_changes`를 서버가 대신 호출

## 제외

- IDE / VS Code extension
- Guide, Nexus, Redis, Kafka, HA
- Tree-sitter Project Intelligence (mock context)
- Phase 1 Local Agent / protocol 재설계
- git commit·push Agent 도구 추가
- Phase 3 이후 범위
- 1일차 V1 DDL 재작성

## 참조 (구현 전 필수)

1. `docs/overview/01_ARCHITECTURE.md`
2. `docs/overview/02_IMPLEMENTATION_RULES.md` §1–3, §6
3. `docs/overview/03_ADR_SUMMARY.md` (ADR-003, ADR-007, ADR-011)
4. `docs/tasks/PHASE_02.md`
5. SPEC-01 Tool Protocol, SPEC-06 Multi-Agent
6. `docs/results/2026-09-16-구현결과.md`

## 완료 기준 (오늘)

1. 1일차 — 충족
2. 2일차 — 충족 (`mvn test` 18건)
3. 3일차 빌드·테스트 확인. Phase 2 DoD 1–10 충족 (gRPC/vLLM은 하니스·스크립트 게이트웨이, 편차 기록) — `mvn test` 29건
4. `docs/results/` 슬라이스 3 + 로그·HANDOFF — 하루 종료 시 갱신

## 리스크

- ADR-003은 gRPC이나 PHASE_02 “IDE 없으면 test harness”가 우선. 전송 어댑터는 Phase 이후.
- Inference는 실모델 없이 스크립트 응답. 진입점은 Gateway 하나로 유지.

## 작업 방식

- 조율·문서·로컬 확인: Grok Bot
- 코드 구현: Cursor Agent (브랜치/PR)
