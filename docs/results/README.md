# 구현·테스트 기록

계획서(`docs/plans/`)가 **무엇을 할지**라면, 이 폴더는 **무엇을 구현했고 어떻게 검증했는지**다.

일별 파일: `YYYY-MM-DD-구현결과.md`. 같은 날짜면 이어서 갱신한다. 슬라이스가 여러 개면 파일 안에 `## 슬라이스 N`으로 나눈다.

채팅이나 일일 로그만으로 끝내지 않는다. 로그는 요약, 여기가 재현 가능한 본문이다.
