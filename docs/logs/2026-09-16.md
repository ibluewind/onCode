# 2026-09-16 작업 로그

- Phase: 2 — Server Workflow Core
- 계획: `docs/plans/2026-09-16-작업계획.md`

## 한 일

1. HANDOFF 기준 Phase 2 착수 결정.
2. `docs/plans/2026-09-16-작업계획.md` 작성 (1일차 슬라이스 정의).
3. Cursor Agent(`ibluewind/onCode`) 기동 시도 → **실패**: Cursor usage 소진, On-demand 필요.
4. 사용자 결정: Cursor Agent 위임 대신 **Cursor IDE로 직접** 1일차 구현.
5. JDK 설치: Eclipse Temurin **25.0.4.1 LTS** (`C:\Program Files\Eclipse Adoptium\jdk-25.0.4.101-hotspot`). Java 27은 2026-09-15 GA이나 non-LTS이고 Spring Boot 4.1은 Java 26까지 지원.
6. `server/` Spring Boot 4.1.1 modular monolith 골격 추가 (8 모듈 패키지 + 경계 인터페이스).
7. onCode Maven은 로컬 CodeGEN Nexus(`127.0.0.1:8081`)를 쓰지 않음. 저장소 `.mvn/settings.xml`이 `~/.m2/settings.xml`의 `mirrorOf *`를 덮어쓰고 Maven Central을 사용.
8. `docs/overview/02_IMPLEMENTATION_RULES.md` 적용 범위 수정: 완성된 onCode로 **사용자 프로젝트**를 구현할 때의 규칙. onCode 저장소 빌드/의존성 정책이 아님. `AGENTS.md`, `docs/overview/README.md` 동기화.
9. `SPEC-14` §63·§150 같은 취지로 수정. 내부 Nexus는 사용자 워크스페이스/폐쇄망 운영 인스턴스. 플랫폼 저장소 개발 빌드는 Maven Central 허용.
10. 코딩 규칙 추가 (언어별): `.cursor/rules/comments.mdc`, `sql-naming.mdc`, `java-naming.mdc`, `go-naming.mdc`, `protocol-naming.mdc`, `typescript-naming.mdc`. `AGENTS.md`·`main_rule.mdc` 연결.
11. 로컬 Postgres를 CodeGEN(5434)과 분리: `infra/docker-compose.yml` → `oncode-postgres` `127.0.0.1:5437`, DB/스키마/사용자 `oncode`. health=healthy, `search_path=oncode, public`.
12. JDBC + Flyway 영속 슬라이스: `work_item` / `workflow` / `workflow_transition` / `context_entry`. `WorkflowEngine` 명시적 전이 RECEIVED → WAITING_DESIGN_APPROVAL. `ContextService` get/put. 리스 만료 후 회수 + WAITING 재시작 resume 테스트.
13. 구현·테스트 기록 폴더 추가: `docs/results/`. 1일차 본문은 `docs/results/2026-09-16-구현결과.md`. 이후 슬라이스는 테스트 직후 여기에 남긴다.
14. 2일차 승인 슬라이스: Flyway V2 `approval`, `ApprovalPort`/`ApprovalService`, `WorkflowEngine.decide`. 설계/코드 구분, 거절·재사용·만료 fail-closed. APPLYING_CHANGE는 상태만 (apply 없음).
15. 3일차: Inference Gateway, 세 에이전트, Tool Gateway + 인프로세스 하니스. mock Design→Approval→Implementation. AGENT apply 금지. `mvn test` 29건.

## 변경 파일 (영속 슬라이스)

- `server/pom.xml` — jdbc, flyway, postgresql, testcontainers
- `server/src/main/resources/application.yml` — datasource 5437, flyway schema `oncode`
- `server/src/main/resources/db/migration/V1__work_item_workflow.sql`
- `server/src/main/java/com/oncode/server/workflow/*` — `WorkflowEngine`, `WorkflowState`, `WorkflowTransitions`
- `server/src/main/java/com/oncode/server/persistence/*` — JDBC 저장소
- `server/src/main/java/com/oncode/server/context/ContextService.java`
- `server/src/test/java/com/oncode/server/workflow/WorkflowEngineTest.java` 등

## 테스트 / 빌드

본문(방식·케이스·중간 실패): `docs/results/2026-09-16-구현결과.md`

```text
JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-25.0.4.101-hotspot
mvn -s .mvn/settings.xml -f server/pom.xml test
```

- Tests run: 29, Failures: 0, Errors: 0, Skipped: 0 (2026-09-16 13:42 KST, 슬라이스 3 포함)
- BUILD SUCCESS (Spring Boot 4.1.1, Java 25.0.4.1, Testcontainers postgres:16-alpine)
- 의존성 출처: `https://repo.maven.apache.org/maven2`
- Flyway는 Testcontainers에 V1+V2 적용. 로컬 5437은 앱 기동 시 동일하게 마이그레이션됨.

## 이슈

오픈 이슈 없음. 구현 중 고친 것:

- Spring Boot 4.1 이중 생성자 → `ContextService`/`WorkflowEngine` 주입 생성자에 `@Autowired`.
- Boot 4.1에 `com.fasterxml.jackson` 없음 → 하니스·출력 검증을 문자열 JSON으로 변경.

## 결정

- onCode 서버 빌드는 머신 로컬 Nexus를 사용하지 않는다. `~/.m2/settings.xml`은 수정하지 않음 (다른 프로젝트용).
- `02_IMPLEMENTATION_RULES`는 onCode **사용 시** 불변조건이다. onCode 자체 Maven Central 사용은 편차가 아니다.
- 코딩 규칙은 컴포넌트(server/local-agent)가 아니라 **언어별**로 둔다. DB는 PostgreSQL snake_case + `COMMENT ON` 필수.
- 로컬 Postgres는 onCode 전용 compose (`127.0.0.1:5437`). CodeGEN 5434 / HRE 5433·5435·5436과 공유하지 않음.
- 워크플로 SSOT는 JPA가 아니라 JDBC + Flyway. 테스트 DB는 Testcontainers, 개발 DB는 compose 5437.
- 구현·테스트는 채팅이 아니라 `docs/results/YYYY-MM-DD-구현결과.md`에 남긴다.
- Phase 2 Tool Gateway는 ADR-003 gRPC 대신 PHASE_02 허용 하니스. Inference는 실 vLLM 없이 스크립트 게이트웨이.

## 계획 대비 차이

- 당초 1일차만 하려다 같은 날 2·3일차까지 진행해 Phase 2 DoD를 닫았다.
- gRPC/실 vLLM은 하니스·스크립트로 대체(편차 기록).

## Phase 2 DoD 갭 (1–10)

| # | 항목 | 오늘 |
| --- | --- | --- |
| 1 | Spring Boot + PostgreSQL 기동 | 충족 (테스트 TC, 로컬 5437) |
| 2 | Work Item / Workflow 영속 | 충족 |
| 3 | WAITING 재시작 lease/recovery | 충족 (테스트) |
| 4 | Context get/put | 충족 |
| 5 | Tool Gateway → Local Agent | 충족 (인프로세스 하니스, gRPC 아님) |
| 6 | Senior / Implementation / Review | 충족 |
| 7 | Inference Gateway만 모델 진입 | 충족 (스크립트, 실 vLLM 아님) |
| 8 | design/code 승인 상태 분리 | 충족 |
| 9 | Agent는 apply 금지 | 충족 |
| 10 | IDE/Guide/Nexus/Redis/HA 없음 | 충족 |

## 하루 종료

- 시각: 2026-09-16 14:01 KST
- 오픈 이슈: 없음
- git commit/push: 하지 않음 (사용자 요청 없음)
- 로컬 compose `127.0.0.1:5437`은 스키마만 있을 수 있음. 테이블은 `spring-boot:run` 때 Flyway. 테스트는 Testcontainers.
- Phase 2 태스크 본문 종료. 다음 세션은 지시 후에만 Phase 3 계획서부터.
