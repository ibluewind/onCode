# 2026-09-14 작업 로그

- Phase: 1 — Local Agent Core
- 계획: `docs/plans/2026-09-14-작업계획.md`
- DoD 점검: `docs/plans/2026-09-14-Phase1-DoD점검.md`
- 시작점: Phase 0 완료 후 Phase 1 착수 (갈래 B)

## 수행한 작업 (시간순)

1. **킥오프** — `local-agent/` 뼈대 (cmd, config, audit, dispatcher, memory transport), workspace 경계, `system.ping` / `get_capabilities`, `workspace.read_file`
2. **ISS-002** — capabilities 계약에 `protocol_version`·`supported_tools` 추가
3. **읽기** — `workspace.read_files`, `workspace.search`
4. **변경** — `propose_changes` + actual unified diff + `ProposalStore`
5. **적용** — `apply_changes` (LocalApproval, stale 거부, 원자 적용·롤백, revision)
6. **Maven** — `process.Run`, `build.run` / `test.run`, sample-java-maven
7. **Git 조회** — `git.status` / `git.diff` (commit/push 없음)
8. **DoD + 통합** — DoD 1–15 OK, read→propose→apply→maven 통합·stale 통합 테스트

## 구현된 도구

| 도구 | 내용 |
|------|------|
| `system.ping` / `system.get_capabilities` | 에이전트·capabilities |
| `workspace.read_file` / `read_files` / `search` | 읽기·검색 |
| `workspace.propose_changes` / `apply_changes` | PROPOSE→APPROVE→APPLY |
| `build.run` / `test.run` | Maven |
| `git.status` / `git.diff` | 로컬 조회만 |

## 테스트 / 검증

```text
go build ./local-agent/cmd/oncode-agent
go test ./local-agent/... ./protocol/...
```

통과 (Maven·통합 시나리오 포함).

## 이슈

- 해결: ISS-002
- 열린 이슈: 없음

## 결정 사항

- JSON 논리 프로토콜 유지; Local Agent는 `oncode/protocol` 재사용
- apply approval은 Phase 1 mock (`LocalApproval`)
- git Agent 도구는 status/diff만
- Phase 1 DoD 충족 → Phase 2는 별도 지시·계획서 후

## 남은 일

- Phase 2 착수는 사용자 지시 대기
- (선택) 커밋은 사용자 요청 시에만
